# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
out/
build/
*.tsbuildinfo

# Turbo
.turbo/

# Tests / coverage
coverage/
playwright-report/
test-results/
blob-report/
.playwright/
# E2e seed data written by global-setup.ts — must not be committed
apps/web/e2e/.e2e-accounts.json

# Prisma
prisma/*.db
prisma/*.db-journal

# Env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Claude pipeline scratch (per-story working artifacts: plan/notes/qa/review/state/ledger)
.claude/pipeline/

# Stray compiled output must never land in shared/src — build emits to packages/shared/dist/
packages/shared/src/*.js
packages/shared/src/*.d.ts

# Design prototype — keep the .dc.html design references in repo; drop heavy editor artifacts
Manga creator collaboration platform/Encre et Plume.html
Manga creator collaboration platform/*.js
Manga creator collaboration platform/.image-slots.state.json
Manga creator collaboration platform/.thumbnail
Manga creator collaboration platform/screenshots/
Manga creator collaboration platform/uploads/
